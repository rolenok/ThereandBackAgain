#include <math.h>
#include <iostream>
#include <transpose.h>

using namespace std;

int conjGrad(int A, b, x) {
	A[i][j] = 0
	int r = b - A*x;
	int r_T = transpose(r);subl 
	int p = r;

	for( k = 1; k < len(b); k++) {
		int Ap[c][d] = A*p;
		
	}


}
